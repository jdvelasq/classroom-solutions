# LAB_03 — Pipeline incremental e idempotente

Actualice el estado vigente de transacciones con un lote diario que trae una corrección y un reenvío. La segunda ejecución del mismo lote no puede cambiar el resultado.
