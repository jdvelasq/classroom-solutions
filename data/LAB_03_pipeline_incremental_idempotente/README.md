# LAB_03 — Pipeline incremental e idempotente

Actualice el estado vigente de operaciones de producción máquina--día con un lote que trae una corrección, un reenvío y una operación nueva. La segunda ejecución del mismo lote no puede cambiar el resultado.
